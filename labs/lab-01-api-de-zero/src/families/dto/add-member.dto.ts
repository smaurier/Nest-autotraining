// add-member.dto.ts — PAGE BLANCHE. Le corps attendu de POST /families/:id/members.
// Export attendu : AddMemberDto, avec `email` (format email) et `role`
// (une des trois valeurs : "admin" | "parent" | "enfant").
export {};
